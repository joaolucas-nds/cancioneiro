# Log — 2026-07-21

## Objetivo da sessão
Verificar o estado real (mount como fonte da verdade) e prosseguir.

## Feito
- Li o `_MANIFEST.md`: 27 arquivos, árvore correta na máquina do usuário (`C:\Users\alexk\Tools\cancioneiro`).
- Descobri que o mount estava na 0.3.0 — SEM o export MD. Investiguei e achei a causa: na sessão anterior implementei o export, commitei no repo do ambiente do assistente e **não entreguei arquivo nenhum**, dizendo ao usuário para "sincronizar". Não existe sincronização entre os dois ambientes. Falha minha.
- Corrigi a DEC-008 com a lição: entregar sempre os arquivos que mudaram, uma vez só (direto se poucos, zip se muitos, nunca os dois).
- Reconstruí o repo a partir do mount (fonte da verdade), confirmei por diff que a única divergência era o export MD, e apliquei sobre a versão real do usuário.
- **FIX-002:** corrigi a duplicação de conceitos no Style recomposto, visível no print do usuário. Dedup passou a ser por contenção.

## Decisões
- DEC-008 revisada (entrega sem duplicatas, mas SEMPRE entregando).
- FIX-002 registrado.

## Bugs
- FIX-002: Style recomposto duplicava conceitos (dedup exato não pegava subconjuntos). Corrigido e testado (8/8).

## Aprendizados / armadilhas
- **Regra de higiene nunca pode custar a entrega.** Cortei o zip e esqueci de entregar de outra forma. Verificar sempre: "o usuário CONSEGUE obter o que produzi?"
- **Teste que espelha a premissa do bug não encontra o bug.** Meu teste de duplicata usava comparação exata — o mesmo critério errado do código. O print do usuário revelou em segundos.
- O mount é a fonte da verdade sobre o que o usuário tem; o repo do ambiente é descartável.

## Onde parei
0.4.0 pronta e entregue de fato: export MD + FIX-002. Publicada pelo usuário no GitHub Pages (0.3.0 ainda no ar até ele atualizar).

## Próximos passos
1. Usuário atualiza os arquivos e republica no Pages.
2. Refino do painel de módulos — opções levantadas: i-011 (busca/autocomplete), Style editável à mão, contador de tags com aviso de limite Suno.
3. Converter um álbum real como dataset.
